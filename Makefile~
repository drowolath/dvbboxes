init:
	python setup.py --command-packages=stdeb.command bdist_deb
	rm -rf build dist .egg nemo.egg-info nemo*.tar.gz
	mv deb_dist/python-nemo_*.deb .
	mv python-nemo_*.deb deb_dist/python-nemo.deb

clean:
	rm -r deb_dist

install:
	dpkg -i deb_dist/python-nemo.deb
